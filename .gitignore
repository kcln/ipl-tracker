venv/
__pycache__/
*.pyc
.DS_Store
data/_cache/
# Recipient list — personal info, kept out of the public repo
recipients.txt
# Opt-out list — same reason
optout.txt
# STOP/START cursor — tracks last-processed iMessage rowid
data/messages_cursor.json
# Heartbeat — tracks when the tracker last ran (every cycle); not real data
data/last_run.txt
